# Garmin Connect Credentials
GARMIN_EMAIL="your-email@example.com"
GARMIN_PASSWORD="your-password-here"

# Sync Configuration
SYNC_INTERVAL_HOURS=12          # How often to auto-sync (default: 12)
ACTIVITY_LOOKBACK_DAYS=360      # How far back to fetch activities (default: 120)
